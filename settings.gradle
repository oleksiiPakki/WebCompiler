rootProject.name = 'web'

include 'servlets'
include 'calculator'

buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath 'com.bmuschko:gradle-cargo-plugin:2.7.1'
    }
}
